package kotoba:app@0.1.0;

/// The first portable Kotoba application world.  It is deliberately pure:
/// authority is added only as a separately named import in a future compatible
/// world revision.
world kotoba-app {
  export main: func() -> s64;
}
